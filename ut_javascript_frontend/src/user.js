class User {
  constructor(post) {
    this.id = user.id;
    this.username = user.username;
    this.email = user.email;
    this.image = user.image;
    Post.all.push(this);
  }

  renderUsers() {
    document;
  }
}

User.all = [];
