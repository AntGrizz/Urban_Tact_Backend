class User {
  constructor(post) {
    this.id = user.id;
    this.username = user.username;
    this.email = user.email;
    this.image = user.image;
    User.all.push(this);
  }

  renderUser(e) {

  }
}

User.all = [];
